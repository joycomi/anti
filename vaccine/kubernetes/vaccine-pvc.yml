apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaccine-disk
  namespace: anticorona
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 1Gi